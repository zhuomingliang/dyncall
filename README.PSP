cross-compile for PSP using PSP-SDK
===================================

  configure

  ./configure --prefix=<path>
  .\setup /prefix <path>


  build
  
  make -f Makefile.psp


  install

  make -f Makefile.psp install


